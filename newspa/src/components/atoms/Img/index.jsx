import React from 'react'
// import PropTypes from 'prop-types'

const Img = ({ ...props }) => {
  return (
    <img {...props} />
  )
}

// Img.propTypes = {
//   children: PropTypes.any,
// }

export default Img
